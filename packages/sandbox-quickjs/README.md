# @codemodekit/sandbox-quickjs

The bounded QuickJS/WASM execution backend for CodeModeKit. It is configured automatically by the batteries-included `codemodekit` package and remains available for expert composition.
